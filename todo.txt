TODO — ESP32-S3 NES Handheld Emulator
======================================

SD CARD
[ ] Debug SD card — ESP_ERR_INVALID_RESPONSE on CMD8 (card responds but wrong check pattern)
    Try: different card, reformat with SD Association formatter, check signal integrity
[ ] Once working: implement ROM browser (list .nes files, thumbstick scroll, A to select)
    Set s_rom_path in main.c and notify emulator_task to start with selected ROM

EMULATOR
[ ] Flash and verify NOFRENDO boots with built-in demo ROM (main_loop(NULL))
[ ] Verify display output — custom_blit in osd.c converts palette→RGB565 and DMAs to ILI9341
[ ] Verify input — buttons and thumbstick feed into NOFRENDO event system via osd_getinput()

CLEANUP
[ ] nofrendo_hal.c — hal_display_frame is now handled by osd.c; decide if component is needed
[ ] hal_audio_push — stub until PCM5102A hardware is added
